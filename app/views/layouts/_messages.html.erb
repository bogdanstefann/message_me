<% flash.each do |message_type, msg| %>
  <div class="ui <%= message_type %> message transition">
    <i class="close icon"></i>
    <div class="header">
      <%= msg %>
    </div>
  </div>
<% end %>
